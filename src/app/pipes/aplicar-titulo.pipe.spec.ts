import { AplicarTituloPipe } from './aplicar-titulo.pipe';

describe('AplicarTituloPipe', () => {
  it('create an instance', () => {
    const pipe = new AplicarTituloPipe();
    expect(pipe).toBeTruthy();
  });
});
