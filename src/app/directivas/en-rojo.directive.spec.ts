import { EnRojoDirective } from './en-rojo.directive';

describe('EnRojoDirective', () => {
  it('should create an instance', () => {
    const directive = new EnRojoDirective();
    expect(directive).toBeTruthy();
  });
});
